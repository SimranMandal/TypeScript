//Regular Function

function addOne(num : number){
    return num + 1;
}

const res = addOne(5);
console.log(res);

//Arrow Function

const multiply = (x : number, y : number) => x * y;
const ans = multiply(2, 50);
console.log(ans);
